[uwsgi]
# Django-related settings
# the base directory (full path)
chdir           = /app
# Django's wsgi file
module          = viva_app.wsgi
# environment variables
env=MYDELICA_CONFIG=/app/viva_config_docker_prod.json
